[project]
name = "supervisor"
version = "0.1.0"
description = "Investment Committee Supervisor: orchestrates crypto and risk agents via A2A"
requires-python = ">=3.12"
dependencies = [
    "google-adk[a2a]>=2.0.0",
    "google-genai>=0.1.0",
    "pydantic>=2.0.0",
    "google-cloud-aiplatform>=1.38.0",
    "python-multipart>=0.0.18",
]

[tool.uv]
package = false
